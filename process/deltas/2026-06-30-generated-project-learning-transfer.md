# Generated Project Learning Transfer Delta

date: 2026-06-30
status: accepted corrective implementation delta

## Trigger

The operator corrected the prior improvement: because RepoKernel is a metatool,
an improvement is ineffective if it remains only in RepoKernel documentation.
The improvement must be transferred into the final object generated or staged
by RepoKernel, instilling the project with the capacity to understand how useful
interactions become durable continuity.

## Correction

RepoKernel now projects project-learning instructions into generated/staged
Project Kernel output:

```text
AGENTS.md adapter delta;
.repokernel/state/CURRENT_STATE.md;
.repokernel/packets/FIRST_PACKET.md;
.repokernel/skills/<project>-semantic-kernel/SKILL.md;
.repokernel/deltas/README.md at L2.
```

The generated object now carries rules for:

```text
turning useful interactions into proposed memory deltas;
preserving accepted corrections and reusable rules;
identifying project-local skill candidates;
rejecting ordinary chat, duplicate summaries and speculation as durable memory;
keeping all promotion behind review.
```

## Boundary

The generated learning protocol is proposal-only. It does not grant:

```text
target write authority;
apply;
runtime hooks;
background automation;
autonomous project intelligence;
Seed/THIA/Lab promotion.
```

## Validation

```text
planner tests assert the generated kernel carries learning protocol;
reference distribution must verify after regeneration;
repokernel-source audit must remain ready.
```
